const initState = {
  identity: "",
};

const reducer = (state = initState, action: any) => {
  switch (action.type) {
    case "ACTION":
      break;

    default:
      return {
        state,
      };
  }
};

export default reducer;